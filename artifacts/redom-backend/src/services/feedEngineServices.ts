/**
 * Build the Home feed.
 */
buildHomeFeed()

/**
 * Build the Following /
 * Friends feed.
 */
buildFollowingFeed()

/**
 * Build the Videos /
 * Reels feed.
 */
buildVideoFeed()

/**
 * Fetch candidate posts.
 */
fetchCandidatePosts()

/**
 * Filter deleted posts.
 */
filterDeletedPosts()

/**
 * Filter archived posts.
 */
filterArchivedPosts()

/**
 * Filter blocked users.
 */
filterBlockedUsers()

/**
 * Filter restricted users.
 */
filterRestrictedUsers()

/**
 * Filter muted users.
 */
filterMutedUsers()

/**
 * Filter expired polls.
 */
filterExpiredPolls()

/**
 * Apply privacy rules.
 */
applyPrivacyRules()

/**
 * Apply audience rules.
 */
applyAudienceRules()

/**
 * Detect AI-generated
 * content.
 */
detectAIGeneratedContent()

/**
 * Verify creator
 * disclosure for
 * AI-generated content.
 */
verifyAIDisclosure()

/**
 * Apply the
 * "AI-generated"
 * label before the
 * post is published.
 */
labelAIGeneratedContent()

/**
 * Score candidate posts.
 */
scorePosts()

/**
 * Apply recommendation
 * signals.
 */
applyRecommendationSignals()

/**
 * Apply AI-assisted
 * ranking.
 */
applyAIRanking()

/**
 * Rank posts.
 */
rankPosts()

/**
 * Remove duplicate
 * posts.
 */
removeDuplicates()

/**
 * Mix different
 * content types.
 */
mixContent()

/**
 * Prioritize friends.
 */
prioritizeFriends()

/**
 * Prioritize followed
 * creators.
 */
prioritizeFollowing()

/**
 * Prioritize trending
 * content.
 */
prioritizeTrending()

/**
 * Prioritize recently
 * published posts.
 */
prioritizeRecent()

/**
 * Record feed
 * impressions.
 */
recordImpressions()

/**
 * Cache feed results.
 */
cacheFeed()

/**
 * Clear expired cache.
 */
clearFeedCache()

/**
 * Load additional
 * content.
 */
loadMore()

/**
 * Continue infinite
 * scrolling.
 */
continueInfiniteFeed()

/**
 * Refresh feed.
 */
refreshFeed()

/**
 * Synchronize feed
 * recommendation data.
 */
synchronizeFeed()

/**
 * Finalize the feed
 * before returning it
 * to the client.
 */
finalizeFeed()