import { Router } from "express";
const router=Router();
const groupPolicy={version:"2.6.0",effectiveAt:"2026-09-14T00:00:00.000Z",sections:[
 {heading:"Groups",body:"ReDom groups support up to 1,024 active members. Every group has an owner, admins and members. The backend is authoritative for membership, admin roles, group settings, invite links and join approval."},
 {heading:"Group permissions",body:"Group admins can independently control Edit group settings, Send new messages, Add other members, Send message history and Invite via link or QR code, plus Approve new members and group admin roles. A client cannot bypass an admin-only permission by changing local state."},
 {heading:"Group invite links and QR codes",body:"ReDom creates cryptographically random group invite tokens, exposes the invite as a ReDom HTTPS/deep link and renders a QR code from that link. Admins can reset an active link; resetting permanently invalidates the previous token. Invite access is checked against current group membership, group size and approval settings."},
 {heading:"Group join approval",body:"When Approve new members is enabled, joining through an invite creates a pending membership request. The requester sees that an admin must approve the request. Admins can approve or reject pending requests. A pending request does not become an active member until approval."},
 {heading:"Adding and removing members",body:"Authorized group members can add people when Add other members is enabled; admins can always add members subject to the 1,024-member limit. Admins can remove members. The owner cannot be removed and must transfer ownership before leaving."},
 {heading:"Group admin roles",body:"The group owner can promote or dismiss admins. The owner remains an admin and ownership can be transferred to another active member before the owner leaves. Role changes are backend-authorized."},
 {heading:"Group member tags",body:"A member can have a private/group-scoped member tag such as a role label. A member can manage their own tag, while admins can manage another member's tag. Tags are ReDom profile/group metadata and are not phone-number identity labels."},
 {heading:"Group member changes",body:"Group management activity can be surfaced as a member-change history using ReDom activity records. This history is authorized to active group members and does not expose private message plaintext."},
 {heading:"Group calls",body:"Group chats expose voice call, video call, participant selection, secure call-link sharing and schedule-call entry points. Live group calls remain backend-authorized and capped at the supported 32-participant call capacity. Only authorized group members may participate in an in-group call."},
 {heading:"Group info",body:"Group info includes group identity, description, media/links/docs navigation, notifications, media visibility, encryption verification, disappearing messages, chat lock, advanced privacy, community actions, member management, favorites/lists, clear chat, exit and report controls. Group identity is presented with ReDom display names rather than phone-number-centric labels."},
 {heading:"Group encryption",body:"Group messages remain end-to-end encrypted with per-device recipient envelopes. Adding a member authorizes that member's active devices for future encrypted messages. A client cannot read historical encrypted content simply by becoming a member; historical-message transfer requires an explicit supported encrypted history workflow."},
 {heading:"Reference behavior",body:"These group behaviors follow the corresponding current WhatsApp Help Center group rules where applicable, including the 1,024-member ceiling, invite-link/QR behavior, reset semantics, member permissions, message-history option and admin approval model. ReDom does not claim a feature is available until its backend and frontend path are implemented."}
]};
router.get("/policies/messaging-v26",(_req,res)=>res.json({success:true,slug:"messaging",...groupPolicy}));
export default router;
