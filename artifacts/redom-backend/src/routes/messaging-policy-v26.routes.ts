import { Router } from "express";
const router=Router();
const groupPolicy={version:"2.7.0",effectiveAt:"2026-09-15T00:00:00.000Z",sections:[{heading:"Groups",body:"ReDom groups support up to 1,024 active members. Every group has an owner, admins and members. The backend is authoritative for membership, admin roles, group settings, invite links and join approval."},{heading:"Group permissions",body:"Group admins can independently control Edit group settings, Send new messages, Add other members, Send message history and Invite via link or QR code, plus Approve new members and group admin roles. A client cannot bypass an admin-only permission by changing local state."},{heading:"Group invite links and QR codes",body:"ReDom creates cryptographically random group invite tokens, exposes the invite as a ReDom HTTPS/deep link and renders a QR code from that link. Admins can reset an active link; resetting permanently invalidates the previous token. Invite access is checked against current group membership, group size and approval settings."},{heading:"Group join approval",body:"When Approve new members is enabled, joining through an invite creates a pending membership request. The requester sees that an admin must approve the request. Admins can approve or reject pending requests. A pending request does not become an active member until approval."},{heading:"Adding and removing members",body:"Authorized group members can add people when Add other members is enabled; admins can always add members subject to the 1,024-member limit. Admins can remove members. The owner cannot be removed and must transfer ownership before leaving."},{heading:"Group admin roles",body:"The group owner can promote or dismiss admins. The owner remains an admin and ownership can be transferred to another active member before the owner leaves. Role changes are backend-authorized."},{heading:"Group member tags",body:"A member can have a private/group-scoped member tag such as a role label. A member can manage their own tag, while admins can manage another member's tag. Tags are ReDom profile/group metadata and are not phone-number identity labels."},{heading:"Group member changes",body:"Group management activity can be surfaced as a member-change history using ReDom activity records. This history is authorized to active group members and does not expose private message plaintext."},{heading:"Group calls",body:"Group chats expose voice call, video call, participant selection, secure call-link sharing and schedule-call entry points. Live group calls remain backend-authorized and capped at the supported 32-participant call capacity. Only authorized group members may participate in an in-group call."},{heading:"Group info",body:"Group info includes group identity, description, media/links/docs navigation, notifications, media visibility, encryption verification, disappearing messages, chat lock, advanced privacy, community actions, member management, favorites/lists, clear chat, exit and report controls. Group identity is presented with ReDom display names rather than phone-number-centric labels."},{heading:"Conversation-level encryption",body:"Every ReDom conversation establishes its encryption context before normal messaging is allowed. A direct chat created after a friend/message relationship is accepted and a group created or admitted through the group flow must establish one conversation-level encryption key before content is sent. The conversation key is generated on an authorized client and wrapped to the active devices of the conversation participants; the server stores only the wrapped key envelopes and key metadata, never the plaintext conversation key."},{heading:"Messages inside an encrypted conversation",body:"Messages, voice messages, photos, videos, documents and other encrypted chat content belong to the conversation encryption context. ReDom does not display an 'Encrypted' placeholder as message content. The client decrypts content locally and renders the actual message. Each encrypted payload uses a fresh AES-GCM nonce under the conversation key; this is cryptographic message framing inside one conversation key, not creation of a new user-visible encryption session for every message."},{heading:"Encryption readiness",body:"A conversation cannot send normal message content until the current device has an encryption identity and the conversation encryption state has been established. If an active participant/device has no registered encryption identity, ReDom must stop the encrypted send rather than silently send plaintext or show an encrypted placeholder."},{heading:"Encryption notice",body:"Once conversation encryption is established, the conversation UI presents the privacy notice: 'Messages and calls are end-to-end encrypted. Only people in this chat can read, listen to, or share them.' The notice is informational system UI and is separate from message bubbles. Learn more opens the conversation encryption verification/details flow."},{heading:"Device authorization",body:"Conversation encryption is distributed to authorized active devices, not phone numbers. A newly registered active device must receive a wrapped conversation key before it can decrypt that conversation. Revoked or inactive devices are not eligible for new conversation-key envelopes."},{heading:"Group membership and encryption",body:"Group membership changes are security-sensitive. New active devices are enrolled into the conversation encryption state before they can receive future encrypted content. Removing a member or changing the authorized device set requires conversation-key rotation before future content is released to the changed membership set; historical plaintext is never reconstructed by the server."},{heading:"Reference behavior",body:"These group and privacy behaviors follow the corresponding current WhatsApp Help Center behavior where applicable, including the 1,024-member ceiling, invite-link/QR behavior, reset semantics, member permissions, message-history option and admin approval model. ReDom's encryption implementation uses its own cryptographic protocol and does not claim to reproduce WhatsApp's private internal key schedule. ReDom does not claim a feature is available until its backend and frontend path is implemented."}]};
router.get("/policies/messaging",(_req,res)=>res.json({success:true,slug:"messaging",...groupPolicy}));export default router;
