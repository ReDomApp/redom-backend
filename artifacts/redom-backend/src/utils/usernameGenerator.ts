/**
 * Generate a unique
 * username.
 */
generateUsername()

/**
 * Normalize a username.
 */
normalizeUsername()

/**
 * Validate username
 * format.
 */
validateUsername()

/**
 * Check username
 * availability.
 */
checkAvailability()

/**
 * Generate username
 * suggestions.
 */
generateSuggestions()

/**
 * Prevent reserved
 * usernames.
 */
blockReservedUsernames()

/**
 * Prevent offensive
 * usernames.
 */
blockProhibitedUsernames()

/**
 * Detect impersonation
 * attempts.
 */
detectImpersonation()

/**
 * Check minimum and
 * maximum length.
 */
validateLength()

/**
 * Validate supported
 * characters.
 */
validateCharacters()

/**
 * Prevent consecutive
 * special characters.
 */
validateSpecialCharacters()

/**
 * Prevent duplicate
 * usernames.
 */
preventDuplicates()

/**
 * Reserve a username
 * during registration.
 */
reserveUsername()

/**
 * Release an unused
 * reserved username.
 */
releaseReservedUsername()

/**
 * Rename a username.
 */
changeUsername()

/**
 * Validate username
 * history.
 */
validateUsernameHistory()