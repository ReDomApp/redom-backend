import { useEffect, useMemo, useState } from "react";
import { ActivityIndicator, FlatList, KeyboardAvoidingView, Modal, Platform, Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import { getLocales } from "expo-localization";
import type { NativeStackScreenProps } from "@react-navigation/native-stack";
import ReDomLogo from "../../assets/brand/redom-logo.svg";
import LockedKey from "../../assets/auth/locked-key.svg";
import InfoBlack from "../../assets/auth/info-black.svg";
import SearchIcon from "../../assets/auth/search.svg";
import CheckWhite from "../../assets/auth/check-white.svg";
import { ReDomScreen } from "../../layout/ReDomScreen";
import { AIText } from "../../i18n/AIText";
import { useLanguage } from "../../i18n/LanguageProvider";
import { localizeUiTexts } from "../../i18n/aiLocalization";
import { authService } from "../../auth/service";
import { getDeviceId } from "../../utils/device";
import { showReDomNotification } from "../../notifications/notificationService";
import { COUNTRIES, type Country } from "../../data/countries";
import type { RegistrationFlowNetworkSecurity } from "../../auth/types";
import type { RootStackParamList } from "../../routing/types";

type Props = NativeStackScreenProps<RootStackParamList, "RegistrationPhone">;
const BLUE = "#1877F2"; const TEXT = "#1C1E21"; const MUTED = "#65676B"; const BORDER = "#CCD0D5"; const ERROR = "#E41E3F";
function maskedFlowId(flowId: string) { const visible = flowId.slice(0, 4); return `${visible}${"•".repeat(Math.max(0, flowId.length - visible.length))}√`; }
function maskIp(ip: string | null) { if (!ip) return "Unavailable"; if (ip.includes(":")) return `${ip.slice(0, 8)}••••••`; const parts = ip.split("."); return parts.length === 4 ? `${parts[0]}.${parts[1]}.•••••` : `${ip.slice(0, 6)}•••••`; }
function digitsOnly(value: string) { return value.replace(/\D/g, ""); }
function dialDigits(country: Country) { return digitsOnly(country.dialCode); }
function networkReason(message: string) { return /^(VPN_DETECTED|TOR_DETECTED|PROXY_DETECTED|DATACENTER_DETECTED|BOT_DETECTED|HIGH_IP_RISK):/.test(message); }
function stripNetworkCode(message: string) { return message.replace(/^(VPN_DETECTED|TOR_DETECTED|PROXY_DETECTED|DATACENTER_DETECTED|BOT_DETECTED|HIGH_IP_RISK):\s*/, ""); }

export function RegistrationPhoneScreen({ navigation, route }: Props) {
  const { t, uiMessage, language } = useLanguage();
  const { reservationId, flowId, expiresAt } = route.params;
  const [secondsLeft, setSecondsLeft] = useState(() => Math.max(0, Math.floor((new Date(expiresAt).getTime() - Date.now()) / 1000)));
  const [country, setCountry] = useState<Country>(COUNTRIES.find((item) => item.code === "NG") ?? COUNTRIES[0]!);
  const [countryOpen, setCountryOpen] = useState(false); const [countrySearch, setCountrySearch] = useState(""); const [phoneInput, setPhoneInput] = useState("");
  const [infoOpen, setInfoOpen] = useState(false); const [learnMoreOpen, setLearnMoreOpen] = useState(false); const [emailNoticeOpen, setEmailNoticeOpen] = useState(false); const [securityOpen, setSecurityOpen] = useState(false);
  const [checking, setChecking] = useState(false); const [checkingStage, setCheckingStage] = useState<"phone" | "security">("phone"); const [error, setError] = useState<string | null>(null); const [saved, setSaved] = useState(false); const [deviceId, setDeviceId] = useState<string | undefined>();
  const [networkBlocked, setNetworkBlocked] = useState(false); const [networkReasonText, setNetworkReasonText] = useState(""); const [securityDetails, setSecurityDetails] = useState<RegistrationFlowNetworkSecurity | null>(null);

  useEffect(() => { const update = () => setSecondsLeft(Math.max(0, Math.floor((new Date(expiresAt).getTime() - Date.now()) / 1000))); update(); const timer = setInterval(update, 1000); return () => clearInterval(timer); }, [expiresAt]);
  useEffect(() => {
    let active = true;
    void (async () => {
      const id = await getDeviceId(); if (!active) return; setDeviceId(id);
      const locale = getLocales()[0]; const deviceRegion = locale?.regionCode?.toUpperCase(); const timeZone = Intl.DateTimeFormat().resolvedOptions().timeZone;
      try { const result = await authService.detectRegistrationFlowPhoneCountry({ reservationId, flowId, deviceId: id, deviceRegion, timeZone }); if (!active || !result.countryCode) return; const detected = COUNTRIES.find((item) => item.code === result.countryCode); if (detected) setCountry(detected); }
      catch { const detected = COUNTRIES.find((item) => item.code === deviceRegion); if (active && detected) setCountry(detected); }
    })();
    return () => { active = false; };
  }, [flowId, reservationId]);

  const countdown = useMemo(() => `${Math.floor(secondsLeft / 60).toString().padStart(2, "0")}:${(secondsLeft % 60).toString().padStart(2, "0")}`, [secondsLeft]);
  const filteredCountries = useMemo(() => { const query = countrySearch.trim().toLowerCase(); if (!query) return COUNTRIES; const normalized = query.replace(/\s+/g, ""); return COUNTRIES.filter((item) => item.name.toLowerCase().includes(query) || item.code.toLowerCase() === normalized.toUpperCase() || item.dialCode.replace(/\D/g, "").includes(normalized)); }, [countrySearch]);
  const phoneDigits = digitsOnly(phoneInput); const canContinue = phoneDigits.length >= 5 && secondsLeft > 0 && !checking && !saved && !networkBlocked;

  function selectCountry(next: Country) { setCountry(next); setCountryOpen(false); setCountrySearch(""); setError(null); setNetworkBlocked(false); if (phoneInput.trim().startsWith("+")) { const raw = digitsOnly(phoneInput); const oldDial = dialDigits(country); if (raw.startsWith(oldDial)) setPhoneInput(raw.slice(oldDial.length)); } }
  function buildE164() {
    const raw = phoneInput.trim(); const digits = digitsOnly(raw); const dial = dialDigits(country); if (!digits) throw new Error("Please enter your mobile number.");
    if (raw.startsWith("+")) { if (!digits.startsWith(dial)) throw new Error("The phone number country code does not match the selected country. Please select the correct country or enter the local number without the country code."); const national = digits.slice(dial.length).replace(/^0+/, ""); if (national.length < 5) throw new Error("Please enter a valid mobile number."); return `+${dial}${national}`; }
    const national = digits.replace(/^0+/, ""); if (national.length < 5) throw new Error("Please enter a valid mobile number."); return `+${dial}${national}`;
  }
  async function continueRegistration() {
    if (!canContinue) return; setChecking(true); setCheckingStage("phone"); setError(null);
    try {
      const phoneNumber = buildE164(); const locale = getLocales()[0]; const deviceRegion = locale?.regionCode?.toUpperCase(); const timeZone = Intl.DateTimeFormat().resolvedOptions().timeZone;
      await authService.saveRegistrationFlowPhone({ reservationId, flowId, deviceId, phoneNumber, countryCode: country.code, deviceRegion, timeZone });
      setCheckingStage("security");
      const security = await authService.inspectRegistrationFlowSecurity({ reservationId, flowId, deviceId });
      setSecurityDetails(security.security); setSaved(true); setNetworkBlocked(false); setNetworkReasonText(""); setSecurityOpen(true);
    } catch (e) {
      const rawMessage = e instanceof Error ? e.message : uiMessage("loginFailed");
      const blocked = networkReason(rawMessage); const cleanMessage = stripNetworkCode(rawMessage);
      if (blocked) { setNetworkBlocked(true); setNetworkReasonText(cleanMessage); void showReDomNotification("ReDom", cleanMessage, { type: "registration-network-blocked", flowId }); }
      try { const [translated] = await localizeUiTexts(language, [cleanMessage], "ReDom signup Screen 4 security result"); if (blocked) setNetworkReasonText(translated || cleanMessage); setError(translated || cleanMessage); } catch { setError(cleanMessage); if (blocked) setNetworkReasonText(cleanMessage); }
    } finally { setChecking(false); }
  }
  function dismissNetworkPrompt() { setNetworkBlocked(false); setError(null); }

  const footer = <View style={styles.footerContent}><View style={styles.loginRow}><Text style={styles.muted}>{t("alreadyAccount")} </Text><Pressable onPress={() => navigation.navigate("Login")} disabled={checking}><Text style={styles.link}>{t("login")}</Text></Pressable></View><View style={styles.companyRow}><ReDomLogo width={72} height={20} /><Text style={styles.company}>{t("company")}</Text></View></View>;
  const securityRows = securityDetails ? [
    ["Your IP", maskIp(securityDetails.ip)], ["Connection", securityDetails.connection || "Unknown"], ["Country", securityDetails.country || securityDetails.countryCode || "Unknown"], ["Region", securityDetails.region || "Unknown"], ["City", securityDetails.city || "Unknown"], ["Organization", securityDetails.organization || "Unknown"], ["ASN", securityDetails.asn ? `AS${securityDetails.asn}` : "Unknown"],
  ] : [];

  return <ReDomScreen footer={footer}>
    <View style={styles.content}>
      <View style={styles.logo}><ReDomLogo width={150} height={42} /></View>
      <View style={styles.flowHeader}><View style={styles.flowRow}><LockedKey width={20} height={20} /><Text style={styles.flowText}>{t("flowId", { flowId: maskedFlowId(flowId) })}</Text></View><Text style={styles.expiry}>{countdown}</Text></View>
      <Text style={styles.title}><AIText context="ReDom signup phone title">What's Your Mobile Number?</AIText></Text>
      <Text style={styles.description}><AIText context="ReDom signup phone description">Enter the mobile number where you can be contacted.</AIText></Text>
      <Text style={styles.smallDescription}><AIText context="ReDom signup phone privacy">Nobody will see this on your profile.</AIText></Text>
      <Pressable accessibilityRole="button" onPress={() => setCountryOpen(true)} disabled={checking || saved || networkBlocked} style={styles.countryBox}><View style={styles.countryTextBlock}><Text style={styles.fieldCaption}><AIText context="ReDom signup country field">Country</AIText></Text><Text style={styles.countryName}>{country.name}</Text></View><Text style={styles.countryDial}>{country.dialCode}</Text></Pressable>
      <View style={styles.phoneBox}><Text style={styles.fieldCaption}><AIText context="ReDom signup phone field">Phone Number</AIText></Text><TextInput value={phoneInput} onChangeText={(value) => { setSaved(false); setSecurityDetails(null); setSecurityOpen(false); setError(null); setNetworkBlocked(false); setPhoneInput(value); }} placeholder="Mobile number" placeholderTextColor="#8A8D91" keyboardType="phone-pad" autoCorrect={false} autoComplete="tel" textContentType="telephoneNumber" editable={!checking && !saved && !networkBlocked} style={styles.input} maxLength={24} /></View>
      <Pressable onPress={() => setInfoOpen(true)} disabled={checking} style={styles.infoRow}><InfoBlack width={20} height={20} /><Text style={styles.infoText}><AIText context="ReDom signup phone notifications">You may receive WhatsApp or SMS notifications from ReDom.</AIText></Text></Pressable>
      <Pressable onPress={() => setLearnMoreOpen(true)} disabled={checking} style={styles.learnMore}><Text style={styles.learnMoreText}><AIText context="ReDom signup phone learn more">Learn More</AIText></Text></Pressable>
      {error && !networkBlocked ? <Text accessibilityRole="alert" style={styles.error}>{error}</Text> : null}
      {saved ? <View style={styles.savedRow}><View style={styles.savedIcon}><CheckWhite width={20} height={20} /></View><Text style={styles.savedText}><AIText context="ReDom signup phone saved">Your mobile number has been securely saved to this registration flow.</AIText></Text></View> : null}
      <Pressable accessibilityRole="button" onPress={() => void continueRegistration()} disabled={!canContinue} style={[styles.button, !canContinue && styles.disabled]}>{checking ? <ActivityIndicator color="#FFFFFF" /> : saved ? <CheckWhite width={24} height={24} /> : <Text style={styles.buttonText}>{t("continue")}</Text>}</Pressable>
      <Pressable accessibilityRole="button" onPress={() => setEmailNoticeOpen(true)} disabled={checking || networkBlocked} style={styles.emailButton}><Text style={styles.emailPrompt}><AIText context="ReDom signup email alternative prompt">Don't have phone number?</AIText></Text><Text style={styles.emailLink}><AIText context="ReDom signup email alternative">Sign up with Email</AIText></Text></Pressable>
      <View style={styles.progress} accessibilityLabel="Registration progress: step 5 of 6">{[0,1,2,3,4,5].map((step) => <View key={step} style={[styles.dot, step <= 4 && styles.dotActive]} />)}</View>
    </View>

    <Modal visible={countryOpen} transparent animationType="slide" onRequestClose={() => setCountryOpen(false)}><KeyboardAvoidingView style={styles.modalBackdrop} behavior={Platform.OS === "ios" ? "padding" : undefined}><View style={styles.countrySheet}><View style={styles.sheetHeader}><Text style={styles.sheetTitle}><AIText context="ReDom country picker title">Choose Country</AIText></Text><Pressable onPress={() => setCountryOpen(false)}><Text style={styles.closeText}>Close</Text></Pressable></View><View style={styles.searchBox}><SearchIcon width={22} height={22} /><TextInput value={countrySearch} onChangeText={setCountrySearch} autoFocus placeholder="Search country or code" placeholderTextColor="#8A8D91" style={styles.searchInput} /></View><FlatList data={filteredCountries} keyExtractor={(item) => item.code} keyboardShouldPersistTaps="handled" showsVerticalScrollIndicator={false} renderItem={({ item }) => <Pressable onPress={() => selectCountry(item)} style={styles.countryRow}><Text style={styles.countryRowName}>{item.name}</Text><Text style={styles.countryRowDial}>{item.dialCode}</Text></Pressable>} ListEmptyComponent={<Text style={styles.emptyText}><AIText context="ReDom country picker empty">No country matches your search.</AIText></Text>} /></View></KeyboardAvoidingView></Modal>
    <Modal visible={infoOpen} transparent animationType="fade" onRequestClose={() => setInfoOpen(false)}><View style={styles.infoBackdrop}><View style={styles.infoCard}><InfoBlack width={42} height={42} /><Text style={styles.infoTitle}><AIText context="ReDom signup phone notifications title">Why you may receive a message</AIText></Text><Text style={styles.infoBody}><AIText context="ReDom signup phone notifications body">You may receive WhatsApp or SMS notifications from ReDom for phone verification, account security, recovery, and important registration updates. Availability depends on your selected contact method and region.</AIText></Text><Pressable onPress={() => setInfoOpen(false)} style={styles.infoButton}><Text style={styles.infoButtonText}>OK</Text></Pressable></View></View></Modal>
    <Modal visible={learnMoreOpen} transparent animationType="fade" onRequestClose={() => setLearnMoreOpen(false)}><View style={styles.infoBackdrop}><View style={styles.infoCard}><InfoBlack width={42} height={42} /><Text style={styles.infoTitle}><AIText context="ReDom signup phone learn more title">Phone contact and account security</AIText></Text><Text style={styles.infoBody}><AIText context="ReDom signup phone learn more body">ReDom verifies the number with Twilio Basic Lookup first and Abstract Phone Validation if Twilio is unavailable. IPAPI then checks the current Internet connection and returns the security and network details used for this registration Flow ID.</AIText></Text><Pressable onPress={() => setLearnMoreOpen(false)} style={styles.infoButton}><Text style={styles.infoButtonText}>OK</Text></Pressable></View></View></Modal>
    <Modal visible={emailNoticeOpen} transparent animationType="fade" onRequestClose={() => setEmailNoticeOpen(false)}><View style={styles.infoBackdrop}><View style={styles.infoCard}><InfoBlack width={42} height={42} /><Text style={styles.infoTitle}><AIText context="ReDom email alternative title">Email sign-up</AIText></Text><Text style={styles.infoBody}><AIText context="ReDom email alternative body">Email sign-up is an available contact option for ReDom. This phone step remains unchanged until the email registration path is selected.</AIText></Text><Pressable onPress={() => setEmailNoticeOpen(false)} style={styles.infoButton}><Text style={styles.infoButtonText}>OK</Text></Pressable></View></View></Modal>
    <Modal visible={checking} transparent animationType="fade"><View style={styles.checkingBackdrop}><View style={styles.checkingCard}><ActivityIndicator size="large" color={BLUE} /><Text style={styles.checkingTitle}>{checkingStage === "phone" ? "Verifying your mobile number" : "Checking your Internet connection"}</Text><Text style={styles.checkingBody}>{checkingStage === "phone" ? "ReDom is validating your number with its phone-validation providers." : "ReDom is receiving the IPAPI security details for this registration Flow ID."}</Text></View></View></Modal>
    <Modal visible={securityOpen} transparent animationType="slide" onRequestClose={() => setSecurityOpen(false)}><View style={styles.securityBackdrop}><View style={styles.securitySheet}><View style={styles.networkHandle} /><Text style={styles.securityTitle}><AIText context="ReDom IPAPI security title">Connection & Security</AIText></Text><Text style={styles.securitySubtitle}><AIText context="ReDom IPAPI security subtitle">Security details returned by IPAPI and saved to your Registration Flow ID.</AIText></Text><View style={styles.securityList}>{securityRows.map(([label, value]) => <View key={label} style={styles.securityRow}><Text style={styles.securityLabel}>{label}</Text><Text style={styles.securityValue}>{value}</Text></View>)}</View><View style={styles.securitySignals}><Text style={styles.signalTitle}>Security signals</Text><Text style={styles.signalText}>VPN: {securityDetails?.vpn ? "Detected" : "Not detected"}  •  Proxy: {securityDetails?.proxy ? "Detected" : "Not detected"}</Text><Text style={styles.signalText}>Tor: {securityDetails?.tor ? "Detected" : "Not detected"}  •  Automated traffic: {securityDetails?.bot ? "Detected" : "Not detected"}</Text><Text style={styles.signalText}>Abuse: {securityDetails?.abuser ? "Flagged" : "Not flagged"}</Text></View><Pressable onPress={() => setSecurityOpen(false)} style={styles.infoButton}><Text style={styles.infoButtonText}>OK</Text></Pressable></View></View></Modal>
    <Modal visible={networkBlocked} transparent animationType="slide" onRequestClose={() => {}}><View style={styles.networkBackdrop}><View style={styles.networkSheet}><View style={styles.networkHandle} /><InfoBlack width={42} height={42} /><Text style={styles.networkTitle}><AIText context="ReDom network security blocked title">Connection change required</AIText></Text><Text style={styles.networkBody}>{networkReasonText}</Text><Text style={styles.networkHint}><AIText context="ReDom network security blocked hint">Disconnect the VPN, Tor, proxy, or datacenter connection identified above. Then reconnect to your normal mobile or residential Internet connection.</AIText></Text><Pressable onPress={() => { dismissNetworkPrompt(); setTimeout(() => void continueRegistration(), 100); }} style={styles.infoButton}><Text style={styles.infoButtonText}><AIText context="ReDom network security recheck">Check connection again</AIText></Text></Pressable><Pressable onPress={dismissNetworkPrompt} style={styles.dismissButton}><Text style={styles.dismissText}><AIText context="ReDom network security dismiss">Close</AIText></Text></Pressable></View></View></Modal>
  </ReDomScreen>;
}

const styles = StyleSheet.create({content:{flex:1,width:"100%",maxWidth:420,alignSelf:"center",justifyContent:"flex-start",paddingTop:20,paddingBottom:0},logo:{alignItems:"center",marginBottom:12},flowHeader:{marginBottom:18},flowRow:{flexDirection:"row",alignItems:"center",justifyContent:"center",gap:7},flowText:{color:BLUE,fontSize:13,fontWeight:"800",letterSpacing:.3},expiry:{color:MUTED,fontSize:13,textAlign:"center",marginTop:5,fontVariant:["tabular-nums"]},title:{color:TEXT,fontSize:27,lineHeight:33,fontWeight:"800",textAlign:"center",marginBottom:7},description:{color:TEXT,fontSize:15,lineHeight:22,textAlign:"center"},smallDescription:{color:MUTED,fontSize:13,lineHeight:19,textAlign:"center",marginTop:3,marginBottom:18},countryBox:{height:64,borderWidth:1.5,borderColor:BORDER,borderRadius:13,paddingHorizontal:14,flexDirection:"row",alignItems:"center",justifyContent:"space-between",backgroundColor:"#FFFFFF"},countryTextBlock:{flex:1},fieldCaption:{color:MUTED,fontSize:11,fontWeight:"700",marginBottom:3},countryName:{color:TEXT,fontSize:15,fontWeight:"700"},countryDial:{color:BLUE,fontSize:16,fontWeight:"800",marginLeft:12},phoneBox:{height:64,borderWidth:1.5,borderColor:BORDER,borderRadius:13,paddingHorizontal:14,justifyContent:"center",marginTop:10,backgroundColor:"#FFFFFF"},input:{color:TEXT,fontSize:16,height:30,padding:0},infoRow:{flexDirection:"row",alignItems:"center",gap:8,marginTop:11},infoText:{flex:1,color:MUTED,fontSize:12,lineHeight:18},learnMore:{alignSelf:"flex-start",marginTop:5,paddingVertical:2},learnMoreText:{color:BLUE,fontSize:13,fontWeight:"800"},error:{color:ERROR,fontSize:13,lineHeight:19,fontWeight:"600",marginTop:10,textAlign:"center"},savedRow:{flexDirection:"row",alignItems:"center",gap:8,marginTop:10},savedIcon:{width:26,height:26,borderRadius:13,backgroundColor:BLUE,alignItems:"center",justifyContent:"center"},savedText:{flex:1,color:BLUE,fontSize:12,lineHeight:17,fontWeight:"700"},button:{height:54,borderRadius:13,backgroundColor:BLUE,alignItems:"center",justifyContent:"center",marginTop:16,width:"100%"},buttonText:{color:"#FFFFFF",fontSize:17,fontWeight:"800"},disabled:{opacity:.55},emailButton:{alignItems:"center",justifyContent:"center",marginTop:12,paddingVertical:5},emailPrompt:{color:MUTED,fontSize:13},emailLink:{color:BLUE,fontSize:14,fontWeight:"800",marginTop:2},progress:{flexDirection:"row",justifyContent:"center",gap:8,marginTop:20},dot:{width:8,height:8,borderRadius:4,backgroundColor:"#D8DCE1"},dotActive:{width:24,backgroundColor:BLUE},footerContent:{width:"100%",alignItems:"center"},loginRow:{flexDirection:"row",justifyContent:"center",alignItems:"center"},muted:{color:MUTED,fontSize:14},link:{color:BLUE,fontSize:14,fontWeight:"800"},companyRow:{flexDirection:"row",alignItems:"center",justifyContent:"center",gap:7,marginTop:9},company:{color:MUTED,fontSize:12},modalBackdrop:{flex:1,backgroundColor:"rgba(0,0,0,.38)",justifyContent:"flex-end"},countrySheet:{height:"82%",backgroundColor:"#FFFFFF",borderTopLeftRadius:24,borderTopRightRadius:24,padding:20},sheetHeader:{flexDirection:"row",alignItems:"center",justifyContent:"space-between",marginBottom:14},sheetTitle:{color:TEXT,fontSize:21,fontWeight:"800"},closeText:{color:BLUE,fontSize:14,fontWeight:"800"},searchBox:{height:48,borderWidth:1.5,borderColor:BORDER,borderRadius:12,flexDirection:"row",alignItems:"center",paddingHorizontal:12,gap:8,marginBottom:8},searchInput:{flex:1,color:TEXT,fontSize:15,height:46},countryRow:{minHeight:54,borderBottomWidth:1,borderBottomColor:"#EEF0F2",flexDirection:"row",alignItems:"center",justifyContent:"space-between"},countryRowName:{flex:1,color:TEXT,fontSize:15},countryRowDial:{color:MUTED,fontSize:14,fontWeight:"700",marginLeft:12},emptyText:{color:MUTED,fontSize:14,textAlign:"center",marginTop:30},infoBackdrop:{flex:1,backgroundColor:"rgba(0,0,0,.38)",justifyContent:"center",padding:20},infoCard:{backgroundColor:"#FFFFFF",borderRadius:22,padding:24,alignItems:"center"},infoTitle:{color:TEXT,fontSize:21,lineHeight:27,fontWeight:"800",textAlign:"center",marginTop:12},infoBody:{color:MUTED,fontSize:15,lineHeight:23,textAlign:"left",marginTop:12},infoButton:{height:50,borderRadius:13,backgroundColor:BLUE,alignItems:"center",justifyContent:"center",width:"100%",marginTop:18},infoButtonText:{color:"#FFFFFF",fontSize:16,fontWeight:"800"},checkingBackdrop:{flex:1,backgroundColor:"rgba(0,0,0,.42)",justifyContent:"center",alignItems:"center",padding:28},checkingCard:{width:"100%",maxWidth:360,backgroundColor:"#FFFFFF",borderRadius:22,padding:28,alignItems:"center"},checkingTitle:{color:TEXT,fontSize:20,fontWeight:"800",textAlign:"center",marginTop:16},checkingBody:{color:MUTED,fontSize:14,lineHeight:21,textAlign:"center",marginTop:8},securityBackdrop:{flex:1,backgroundColor:"rgba(0,0,0,.45)",justifyContent:"flex-end"},securitySheet:{backgroundColor:"#FFFFFF",borderTopLeftRadius:26,borderTopRightRadius:26,padding:24,paddingBottom:30},securityTitle:{color:TEXT,fontSize:24,lineHeight:30,fontWeight:"800",textAlign:"center",marginTop:4},securitySubtitle:{color:MUTED,fontSize:13,lineHeight:19,textAlign:"center",marginTop:6},securityList:{marginTop:18,borderTopWidth:1,borderTopColor:"#EEF0F2"},securityRow:{minHeight:43,borderBottomWidth:1,borderBottomColor:"#EEF0F2",flexDirection:"row",alignItems:"center",justifyContent:"space-between",gap:14},securityLabel:{color:MUTED,fontSize:13,fontWeight:"600"},securityValue:{color:TEXT,fontSize:14,fontWeight:"700",textAlign:"right",flex:1},securitySignals:{marginTop:14,padding:14,borderRadius:14,backgroundColor:"#F5F6F7"},signalTitle:{color:TEXT,fontSize:14,fontWeight:"800",marginBottom:6},signalText:{color:MUTED,fontSize:12,lineHeight:19},networkBackdrop:{flex:1,backgroundColor:"rgba(0,0,0,.42)",justifyContent:"flex-end"},networkSheet:{backgroundColor:"#FFFFFF",borderTopLeftRadius:26,borderTopRightRadius:26,padding:24,paddingBottom:30,alignItems:"center"},networkHandle:{width:42,height:5,borderRadius:3,backgroundColor:"#CCD0D5",marginBottom:18},networkTitle:{color:TEXT,fontSize:22,lineHeight:28,fontWeight:"800",textAlign:"center",marginTop:10},networkBody:{color:TEXT,fontSize:15,lineHeight:22,textAlign:"center",marginTop:12},networkHint:{color:MUTED,fontSize:13,lineHeight:20,textAlign:"center",marginTop:10},dismissButton:{marginTop:10,paddingVertical:10,paddingHorizontal:20},dismissText:{color:BLUE,fontSize:15,fontWeight:"800"}});
