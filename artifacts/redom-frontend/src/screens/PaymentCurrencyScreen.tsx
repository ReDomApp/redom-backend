import React,{useEffect,useState}from"react";import{SafeAreaView,View,Text,Pressable,StyleSheet,ActivityIndicator,Alert}from"react-native";import{useNavigation,useRoute}from"@react-navigation/native";import{useTheme}from"../theme/ThemeProvider";import{ordersPaymentsService}from"../services/ordersPaymentsService";export function PaymentCurrencyScreen(){const n=useNavigation<any>();const r=useRoute<any>();const{colors}=useTheme();const code=r.params?.selectedCurrency;const[loading,setLoading]=useState(!!code);useEffect(()=>{if(!code){n.goBack();return;}setLoading(false);},[code]);const save=async()=>{if(!code)return;setLoading(true);try{await ordersPaymentsService.updateSettings({currency:code});n.pop(2);}catch(e){Alert.alert("Change currency?",e instanceof Error?e.message:"You can only change your currency once every 72 hours.",[{text:"CANCEL",style:"cancel"},{text:"OK"}])}finally{setLoading(false)}};return <SafeAreaView style={[s.root,{backgroundColor:colors.background}]}><View style={[s.header,{backgroundColor:colors.surface,borderBottomColor:colors.border}]}><Pressable onPress={()=>n.goBack()}><Text style={[s.back,{color:colors.text}]}>‹</Text></Pressable><Text style={[s.title,{color:colors.text}]}>Change currency</Text></View><View style={s.content}><Text style={[s.heading,{color:colors.text}]}>Change your currency to {code}</Text><Text style={[s.copy,{color:colors.textSecondary}]}>You can only change your currency once every 72 hours.</Text><Pressable disabled={loading} onPress={()=>void save()} style={[s.button,{backgroundColor:colors.primary}]}><Text style={s.buttonText}>{loading?"Saving…":"Continue"}</Text></Pressable></View></SafeAreaView>}const s=StyleSheet.create({root:{flex:1},header:{height:58,borderBottomWidth:1,flexDirection:"row",alignItems:"center",paddingHorizontal:14},back:{fontSize:40,width:42},title:{fontSize:19,fontWeight:"800"},content:{padding:30},heading:{fontSize:25,fontWeight:"900"},copy:{fontSize:17,lineHeight:24,marginTop:12},button:{marginTop:30,padding:16,borderRadius:8,alignItems:"center"},buttonText:{color:"#fff",fontSize:17,fontWeight:"800"}});