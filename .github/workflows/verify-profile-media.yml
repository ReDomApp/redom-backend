name: Verify profile media
on:
  push:
    branches:
      - main
jobs:
  verify:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: pnpm install --no-frozen-lockfile
      - run: pnpm --filter @workspace/redom-frontend run typecheck
      - run: pnpm --filter @workspace/redom-backend run build
      - name: Commit lockfile if install updated it
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add pnpm-lock.yaml
          git diff --cached --quiet && exit 0
          git commit -m "Sync workspace lockfile"
          git push
